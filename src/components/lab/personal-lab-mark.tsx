import { FlaskConical } from "lucide-react";

export function PersonalLabMark() {
  return (
    <span className="personal-lab-mark" aria-hidden="true">
      <FlaskConical size={28} strokeWidth={1.6} />
    </span>
  );
}
